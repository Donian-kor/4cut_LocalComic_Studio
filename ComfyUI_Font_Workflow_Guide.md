# ComfyUI 한국어 만화/웹툰 폰트 합성 워크플로우 가이드

본 문서는 로컬 하드웨어 사양(VRAM)의 한계를 극복하고, 만화 이미지 위에 오타 없이 정확한 한국어 대사를 원하는 폰트(TTF)로 합성하기 위한 ComfyUI 파이프라인 구조를 설명합니다.

---

## 1. 전체 워크플로우 논리 구조 (Pipeline Overview)

전체 구조는 이미지 생성을 담당하는 **[베이스 파트]**와 글자 합성을 담당하는 **[텍스트 오버레이 파트]**가 직렬로 연결되는 구조입니다.

```
[1. 이미지 생성 파트]
만화 특화 모델 (SDXL 등) -> KSampler -> VAE Decode -> [배경 만화 이미지 출력]
                                                               ↓
[2. 텍스트 레이어 생성 파트]                                    ↓ (이미지 입력)
로컬 한글 폰트(TTF) 경로 + 한국어 대사 입력 -> Draw Text 노드 -> Image Blend / Overlay 노드
                                                               ↓
                                                    [3. 최종 완상본 저장]
```

---

## 2. 핵심 필수 노드 및 연결 방법

텍스트 합성을 완벽하게 제어하기 위해 커스텀 노드 팩인 **`ComfyUI-Essential-Nodes`** 또는 **`ComfyUI_Custom_Nodes_AlekPet`** 설치가 필요합니다. 본 가이드는 가장 범용적인 **Essential Nodes** 기준으로 설명합니다.

### ① Draw Text (또는 Text Image) 노드
*   **역할:** 입력한 한국어 텍스트 데이터를 지정한 폰트와 크기로 계산하여 투명 또는 단색 배경의 '글자 이미지'로 변환합니다.
*   **주요 설정 필드:**
    *   `text`: 합성할 한국어 대사 입력 (예: `"말도 안 돼...!"`)
    *   `font_path`: 로컬 PC의 실제 폰트 파일 절대 경로 입력 (예: `C:\Windows\Fonts\NanumGothic.ttf`)
    *   `font_size`: 글자 크기 지정 (말풍선 크기에 맞게 조절)
    *   `width` / `height`: 배경 이미지와 동일한 해상도 설정 (예: 1024x1024)

### ② Image Blend (또는 Overlay) 노드
*   **역할:** 만화 이미지(배경) 위에 글자 이미지(전경)를 자연스럽게 합성합니다.
*   **입력 연결:**
    *   `image_a` (Background): VAE Decode에서 나온 오리지널 만화 이미지 연결
    *   `image_b` (Foreground): Draw Text 노드의 `IMAGE` 출력 연결
*   **주요 설정 필드:**
    *   `blend_mode`: `normal` 또는 `multiply` (텍스트 배경이 투명할 경우 `normal` 사용)
    *   `opacity`: `1.0` (글자를 선명하게 표현)

### ③ Image Transform (또는 Position) 노드 (필요시 추가)
*   **역할:** 글자가 말풍선의 정중앙에 위치하도록 x축, y축 좌표를 이동시킵니다.
*   **위치:** `Draw Text` 노드와 `Image Blend` 노드 사이에 배치하여 글자의 위치를 미세 조정합니다.

---

## 3. 한글 폰트 적용 시 주의사항

1.  **경로 지정 오류 방지:** ComfyUI는 역슬래시(`\`)를 인식할 때 오류가 날 수 있으므로, 폰트 경로를 입력할 때는 슬래시(`/`)로 변경하거나 이중 역슬래시(`\\`)를 사용해야 합니다.
    *   *올바른 예시:* `C:/Windows/Fonts/batang.ttc` 또는 `C:\\Windows\\Fonts\\batang.ttc`
2.  **상업적 이용 가능 폰트 추천:** 웹툰이나 만화를 외부 플랫폼에 게시할 목적이라면 **눈누(noonnu.cc)** 등에서 다운로드한 '눈느 만화체', '네이버 나눔손글씨', '주아체' 등 상업적 무료 라이선스 폰트를 사용하는 것을 권장합니다.

---

## 4. 추천 이점 (Advantage)

*   **VRAM 절약:** 텍스트를 그리기 위해 무거운 FLUX 12B 모델을 쓸 필요 없이, 4GB~8GB VRAM 카드로도 SDXL 만화 모델을 사용하여 최고 속도로 작업할 수 있습니다.
*   **오타 0%:** 생성형 AI 특유의 텍스트 깨짐, 자모음 분리, 영문 오인 현상이 원천적으로 차단됩니다.
*   **수정 용의성:** 이미지가 마음에 들면 이미지 생성 프로세스는 고정(Seed 고정)해 두고, 대사 내용과 위치만 바꾼 뒤 즉시 재출력할 수 있어 마감 속도가 극대화됩니다.