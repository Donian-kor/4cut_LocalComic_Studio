# 4Cut Studio UI/UX 디자인 명세

## 1. 전체 레이아웃

```text
┌──────────────────────────────────────────────────────────┐
│ ✦ 4cut Studio   ● 상태                                  │
├──────────────┬───────────────────────────────────────────┤
│              │                                           │
│ ＋ 새 채팅    │              채팅 영역                    │
│              │                                           │
│ 대화 히스토리 │  사용자 메시지                            │
│              │  AI 스토리 계획 카드                       │
│              │  1컷 생성중 카드                          │
│              │  1컷 결과 카드                            │
│              │  2컷 생성중 카드                          │
│              │  ...                                      │
│              ├───────────────────────────────────────────┤
│ ⚙ 설정       │ 고정 Composer                             │
└──────────────┴───────────────────────────────────────────┘
```

- 헤더는 유지한다. 메인 우측 상단의 중복 설정 버튼은 두지 않으며 설정은 사이드바에서만 연다.
- 중앙은 사이드바와 메인 채팅의 수평 `QSplitter`다.
- 메인은 채팅 스크롤 영역과 고정 Composer로 구성한다.
- 사이드바와 채팅은 각각 독립 스크롤을 사용한다.

## 2. 채팅 메시지

- 사용자 메시지는 우측, AI 메시지는 좌측이다.
- 메시지와 말풍선은 분리된 위젯 계층으로 구성한다.
- 긴 텍스트는 자동 줄바꿈한다.
- 새 메시지 추가 시 사용자가 하단을 보고 있을 때만 자동 스크롤한다.

## 3. 스토리 계획 카드

스토리 생성 카드는 생성 완료 후에도 **삭제하지 않고 이번 만화의 설계도** 역할로 유지할 수 있도록 설계한다.

표시 항목:

- 제목
- 분위기 / 그림체
- 등장인물 이름과 외형 요약
- 전체 스토리 요약
- 1~4컷 장면/대사 요약
- 현재 진행 컷 하이라이트

LM Studio 스트리밍 원문(JSON)은 직접 보여주지 않고, UI용 요약 정보만 단계적으로 표시한다.

## 4. 컷별 순차 생성

생성 순서는 반드시 다음과 같다.

```text
1컷 생성 → 이미지 + 대사 합성 완료 → 1컷 표시
2컷 생성 → 이미지 + 대사 합성 완료 → 2컷 표시
3컷 생성 → 이미지 + 대사 합성 완료 → 3컷 표시
4컷 생성 → 이미지 + 대사 합성 완료 → 4컷 표시
최종 4컷 합성
```

생성 중에는 현재 컷 1개만 생성 카드로 표시한다. 생성 카드의 테두리는 펄스/글로우 애니메이션으로 현재 작업 중임을 표현한다.

## 5. 개별 컷 결과 카드

완료된 각 컷은 독립된 결과 카드로 표시한다.

```text
┌────────────────────────────┐
│ 2컷 완성       ✓ 완료      │
│                            │
│        [ 2컷 이미지 ]      │
│                            │
│   "커피 어디 갔지?"        │
│                            │
│ [↻ 이 컷 다시 만들기]      │
│ [✎ 이 컷 수정]             │
└────────────────────────────┘
```

`이 컷 다시 만들기`는 기존 장면/캐릭터/스타일/모델 설정을 유지하고 해당 컷만 새 seed로 다시 생성한다.

`이 컷 수정`은 사용자의 수정 요청을 해당 컷의 revision prompt에 추가하고 해당 컷만 다시 생성한다.

두 경우 모두 완료 후 최종 4컷 합성을 자동 실행한다.

## 6. 생성 일관성

한 만화에서 다음 값은 생성 시작 시 확정해 유지한다.

- Master Seed
- Character Prompt
- Style Prompt
- 이미지 모델
- 해상도
- Steps / CFG
- Sampler / Scheduler
- Negative Prompt

외형/그림체 프롬프트는 하드코딩하지 않는다. 사용자의 분위기/그림체 선택과 LM Studio의 캐릭터 정보를 기반으로 해당 만화에서 한 번 확정하고 모든 컷에 공유한다.

개별 컷 재생성에서는 Master Seed는 세션의 기준값으로 유지하되 해당 컷에만 revision seed를 사용한다.

## 7. Composer

- `QPlainTextEdit` 기반
- Enter 전송 / Shift+Enter 줄바꿈
- 분위기와 그림체 선택 유지
- AI 작업 상태 라벨은 일반 상태보다 약 2pt 크게 표시하고, 생성 완료 시 녹색 상태로 변경한다.
- 생성 중 Composer 비활성화
- 빈 입력 전송 방지

## 8. 세션

- 새 채팅
- 선택/복원
- 이름 변경
- 삭제

생성 중 세션은 삭제와 이름 변경을 비활성화한다. 생성 작업이 끝나거나 취소/실패하면 다시 활성화한다.

세션은 메시지뿐 아니라 개별 컷 재생성에 필요한 Comic 계획 데이터도 저장할 수 있어야 한다.

## 9. 최종 결과

최종 4컷 결과 카드에는 다음을 제공한다.

- 최종 2×2 합성 이미지
- 저장
- 전체 다시 만들기
- 각 컷의 개별 재생성/수정 액션

개별 컷 재생성 완료 후 최종 결과 이미지는 자동으로 업데이트한다.

## 10. 배경 유지

각 컷의 배경은 LM Studio가 생성한 장면/이미지 프롬프트를 기준으로 유지한다. 배경을 과도하게 상세하게 그릴 필요는 없지만 장면을 식별할 수 있는 환경 정보는 반드시 남긴다. 스토리에서 흰색 배경을 명시하지 않았다면 빈 배경이나 순백색 배경으로 임의 대체하지 않는다.
