[1mdiff --git a/app/main_controller.py b/app/main_controller.py[m
[1mindex 062ac79..ed5cbcd 100644[m
[1m--- a/app/main_controller.py[m
[1m+++ b/app/main_controller.py[m
[36m@@ -17,6 +17,7 @@[m [mclass MainController(QObject):[m
         self.result = ResultController(main_window.result)[m
         self.last_idea = ""[m
         self.last_style = ""[m
[32m+[m[32m        self.workers = []  # 실행 중인 ComicWorker 참조 보관 (조기 GC 방지)[m
         main_window.idea.generateRequested.connect(self.start_generation)[m
         main_window.generation.cancelRequested.connect(self.cancel_generation)[m
         main_window.result.regenerateRequested.connect(self.regenerate)[m
[36m@@ -37,12 +38,27 @@[m [mclass MainController(QObject):[m
         self.window.idea.set_enabled(False)[m
         worker = ComicWorker(self.service, self.last_idea, self.last_style)[m
         self.generation.attach_worker(worker)[m
[32m+[m[32m        self.workers.append(worker)[m
         worker.finished_comic.connect(self.on_finished)[m
         worker.failed.connect(self.on_failed)[m
         worker.cancelled.connect(self.on_cancelled)[m
[31m-        worker.finished.connect(worker.deleteLater)[m
[32m+[m[32m        worker.finished.connect(self.release_worker)[m
         worker.start()[m
 [m
[32m+[m[32m    def release_worker(self):[m
[32m+[m[32m        """스레드가 완전히 끝난 뒤에만 ComicWorker 참조를 놓는다.[m
[32m+[m
[32m+[m[32m        run() 이 반환되기 전에 ComicWorker 참조가 사라지면 QThread 객체가[m
[32m+[m[32m        파괴되면서 'QThread: Destroyed while thread ... is still running' 과 함께[m
[32m+[m[32m        프로세스가 즉시 종료된다.[m
[32m+[m[32m        """[m
[32m+[m[32m        worker = self.sender()[m
[32m+[m[32m        if worker is None:[m
[32m+[m[32m            return[m
[32m+[m[32m        worker.wait()[m
[32m+[m[32m        if worker in self.workers:[m
[32m+[m[32m            self.workers.remove(worker)[m
[32m+[m
     def cancel_generation(self):[m
         self.generation.cancel()[m
 [m
