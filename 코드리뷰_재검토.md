# 현재 코드 전수 재검토 및 개선안

검토 기준: 현재 워크스페이스 소스, 설정, workflow, 저장 데이터 구조, UI 명세, 기존 테스트 파일을 정적으로 대조했다. **이번 재검토에서는 파일을 수정하거나 테스트를 실행하지 않았다.** `코드리뷰.md`에 적힌 “11개 테스트 통과”는 기존 문서의 주장으로 기록된 것이며, 이 재검토에서 재실행해 확인한 결과는 아니다.

## 요약

기존 재검토의 핵심 판단 대부분은 현재 코드와 맞는다. 다만 우선순위는 “대사 합성 실패”보다 **재생성 도중 저장되는 부분 상태의 정합성**과 **세션 복구 가능성**을 먼저 다루는 편이 낫다. 현재 구조에서 정상 생성의 중간 산출물은 패널 경로와 채팅 카드에 남을 수 있지만, `comic_data`는 마지막 합성 성공 시점에 저장된다. 프로세스가 중간에 종료되면 보이는 결과와 재생성에 쓰는 계획 데이터가 어긋날 수 있다.

현재 기본 이미지 모델 프로필 두 개에는 각각 stage2 workflow가 존재한다. 따라서 stage2 누락은 기본 설정에서 매번 발생하는 문제가 아니라, stage2 파일이 없는 사용자 workflow를 선택한 경우에만 발생한다. 대사 합성 예외도 최종 합성에서 Pillow fallback이 실행되어 결과 이미지가 생성될 수 있다. 다만 패널 결과 카드가 합성 완료로 표시되고, 실제 stage2 실패 여부를 세션에 남기지 않는 결함은 남아 있다.

## 검토 범위와 기준

- 앱 진입 및 서비스 조립: `app.py`, `app/main_controller.py`
- 도메인·저장: `core/models/`, `core/services/session_manager.py`
- 이미지·대사·합성: `core/services/comic_service.py`, `image_service.py`, `compose_service.py`, `bubble_detector.py`, `compose/`
- 비동기 작업: `core/workers/`, controller 연결부
- 외부 연동: `integrations/comfyui/`, `integrations/lmstudio/`
- UI 흐름: `ui/main/main_window.py`, `ui/chat/chat_widgets.py`, 생성·설정 화면
- 모델 설정·기본 설정·workflow JSON, `requirements.txt`, `.gitignore`, 기존 테스트 파일
- 기대 동작 비교: `디자인.md`, README

문서에 적힌 결론은 그대로 지침으로 실행하지 않고 코드 근거를 기준으로 다시 판정했다. 기존 사용자 변경분(`projects/.sessions/sessions.json`, `projects/2026-09-23_111802_485/`, `코드리뷰.md`)은 건드리지 않았다.

## 우선순위별 개선 항목

### P1 — 재생성 결과와 세션 저장을 하나의 완료 지점으로 묶기

**판정: 실제 결함, 우선 수정 권장.**

`PanelRegenerationWorker.run()`은 새 패널 생성을 마치면 `panel_completed`를 먼저 보내고, 이어서 최종 4컷 합성을 한다. UI의 `complete_panel_generation()`은 이 신호를 받자마자 새 패널 경로와 결과 카드를 세션에 저장한다. 반면 `comic_data`와 최종 `result_path`는 `finish_panel_regeneration()`에서만 갱신된다. 따라서 합성 실패 또는 그 사이 취소 시 `panel_paths`·메시지에는 새 패널이 기록되고 `comic_data`에는 이전 패널이 남는다. 앱 재시작 후 저장된 계획으로 복원하면 이전 이미지가 다시 선택될 수 있다.

**개선안:** 재생성 작업을 준비·적용·완료 단계로 나누고, 새 이미지 파일은 별도 revision 경로에 생성한다. 최종 합성까지 성공한 뒤 패널 데이터, 결과 경로, 카드 상태, `comic_data`를 한 번에 세션에 반영한다. 합성이 실패하거나 취소되면 기존 패널과 결과를 유지하고 새 파일은 고아 산출물로 기록하거나 정리한다. 파일 정리는 세션 커밋 이후에만 수행한다.

**완료 기준:** 합성 실패와 합성 직전 취소 모두 재시작 전후 `comic_data`, `panel_paths`, 결과 이미지가 같은 revision을 가리킨다. 실패한 새 이미지가 성공한 결과처럼 UI에 남지 않는다.

### P1 — 일반 생성도 계획과 부분 결과를 즉시 복구할 수 있게 저장

**판정: 기존 재검토에서 다루지 않은 복구성 공백.**

`MainController._on_planned()`는 seed·prompt·모델 설정만 세션에 저장한다. `session.comic_data`는 정상 완료 또는 패널 재생성 완료에서 갱신된다. 일반 4컷 생성 중 프로세스가 종료되면 이미 끝난 패널의 경로와 카드 메시지는 일부 저장돼도 캐릭터·장면·대사·전체 패널 계획은 복구용 `comic_data`에 없다. `SessionManager.load()`는 재시작 뒤 `generating` 상태를 `cancelled`로 바꾸므로, 이 세션에서 패널 재생성 기능이 요구하는 계획 데이터를 찾지 못할 수 있다.

**개선안:** 계획 수신 시점에 `comic.to_dict()`를 저장하고, 패널마다 이미지 생성·대사 합성 상태가 확정된 후 같은 comic 데이터를 갱신한다. 생성 중단은 마지막으로 커밋된 패널까지 복구 가능한 상태로 표시한다. 자동 저장량이 우려되면 메시지마다 전체 세션 스냅샷을 쓰지 말고, comic/session 단위 변경 묶음으로 저장한다.

**완료 기준:** 생성 1~3컷 중 앱을 재시작해도 저장된 패널을 유지하고, 해당 컷을 재생성할 수 있다. 아직 이미지가 없는 컷은 명시적인 미완료 상태로 보인다.

### P1 — 취소 요청에서 UI 스레드의 네트워크 호출 제거

**판정: 실제 UI 멈춤 가능성.**

`MainController.cancel_generation()`이 UI 스레드에서 `worker.cancel()`을 호출한다. 두 worker의 `cancel()`은 취소 플래그를 세운 뒤 `ComicService.cancel_image_generation()` → `ComfyUIClient.interrupt()`를 직접 호출한다. `/interrupt` 요청 timeout은 3초이므로 ComfyUI가 응답하지 않을 때 UI가 그 시간 동안 멈출 수 있다. 동시에 `wait_for_image()`도 취소 플래그를 감지하면 다시 `interrupt()`를 호출하므로 중복 요청 경로가 있다.

**개선안:** UI에서는 취소 플래그만 설정한다. ComfyUI 대기 루프를 실행 중인 worker 스레드가 취소를 감지하고 interrupt를 한 번 보내도록 소유권을 단일화한다. HTTP 요청이 실제로 끝날 때까지 걸리는 최대 지연과 취소 상태 전이를 명확히 한다.

### P1 — 손상된 세션 하나가 전체 세션 목록을 비우지 않게 복구

**판정: 실제 데이터 접근성 문제.**

`SessionManager.load()`가 `sessions.json` 전체를 하나의 `try`에서 파싱한다. 목록 중 한 세션의 `master_seed` 또는 패널 `seed`가 정수가 아니면 `ChatSession.from_dict()`/`Panel.from_dict()`에서 예외가 나고 전체 `self.sessions`가 빈 목록으로 바뀐다. 파일 자체는 남지만 앱은 정상 세션을 하나도 불러오지 않는다. `SettingsManager._load()`도 JSON 오류를 빈 설정으로 처리하고, 뒤의 저장이 원본을 덮을 수 있어 설정 파일에도 비슷한 복구 위험이 있다.

**개선안:** 파일 JSON 파싱과 각 레코드 검증을 분리한다. 정상 항목은 계속 로드하고 손상 항목은 원본 보존 또는 별도 격리 파일로 백업한 뒤 로그와 UI 상태에 복구 사실을 남긴다. 필드별 기본값 적용은 명시적 검증 규칙을 둔다. 설정 저장도 임시 파일 작성 후 교체 방식으로 원본 손상 가능성을 줄인다.

**완료 기준:** 정상 항목 N개와 손상 항목 1개가 섞여도 N개는 보이며, 손상 원본을 추후 진단할 수 있다. 잘못된 설정을 빈 설정으로 저장해 덮지 않는다.

### P2 — 대사 합성 성공 여부를 패널 카드와 세션에 반영

**판정: 부분 인정.**

`ComicService.generate_panel()` 및 `regenerate_panel()`은 대사 합성 오류를 출력만 하고 계속 진행한다. 일반 worker는 이미지 경로만 `panel_completed`로 전달하며, UI는 `complete_panel_generation()`에서 카드 문구를 대사 합성 완료로 고정한다. 다만 최종 `ComposeService.compose()`가 `dialogue_composited == False`인 패널에 Pillow 대사를 그리므로, 이 예외만으로 대사 없는 최종 이미지를 받는다는 주장은 맞지 않는다. fallback도 실패하거나 합성 단계 전체가 실패하는 경로는 별도로 오류 처리해야 한다.

**개선안:** 합성 결과를 `composited`, `fallback`, `failed` 등 분명한 상태로 반환하고 패널 모델·세션·카드에서 동일하게 표현한다. stage2 예외를 조용히 삼키지 말고 사용자에게 fallback 사용 여부를 알린다. 재시도는 일시 오류 유형을 구분하고 횟수를 제한해 선택적으로 적용한다.

### P2 — stage2 workflow 존재 여부를 서비스 조립 시 확정

**판정: 커스텀 workflow 한정의 낭비와 오류.**

`app.py`는 workflow 경로에서 무조건 `_stage2.json` 경로를 만든다. `WorkflowAdapter.__init__()`는 존재하지 않는 경로도 보관하고, `ComicService`는 경로가 truthy하면 stage2를 시도한다. 기본 프로필의 두 stage2 JSON은 현재 존재하므로 기본 실행에서 반복 오류가 나지는 않는다. stage2가 없는 커스텀 프로필이면 YOLO 탐지와 이미지 업로드 뒤 `prepare_stage2()`에서 `FileNotFoundError`가 발생한다.

**개선안:** 서비스 생성 단계에서 stage2 파일 존재 여부를 검증해 `None`으로 정규화하고, UI 상태 또는 로그에서 대사 합성 방식이 비활성인 이유를 알린다. 프로필 파일 검증을 저장 시 또는 선택 시 수행하면 생성 중 뒤늦은 실패도 막을 수 있다.

### P2 — 전체 재생성에 저장된 생성 설정을 사용

**판정: 세션 재현성 저하.**

패널 재생성은 `_service_for_session()`에서 `generation_config.id`를 사용하고 저장한 `comic_data`를 기반으로 한다. 전체 재생성은 `regenerate()`에서 `last_idea`, `last_mood`, `last_art_style`로 상태를 복원하고, `_style_from_labels()`로 style prompt를 다시 만든다. 저장된 `session.style_prompt`, `character_prompt`, `generation_config`는 이 경로에서 재사용하지 않는다. 설정/프리셋이 바뀐 뒤 전체 재생성을 누르면 처음 생성과 다른 prompt나 모델이 선택될 수 있다.

**개선안:** “동일 설정으로 다시 생성”은 세션에 저장한 설정 스냅샷을 기본으로 사용한다. 사용자가 모델·스타일 변경을 선택한 경우에만 새 설정으로 교체하고 이를 세션에 기록한다. 캐릭터 외형과 panel plan까지 다시 생성하는 동작인지, 기존 계획을 유지하는 동작인지 UI 명칭과 동작을 일치시킨다.

### P2 — 채팅 자동 스크롤을 하단 근처에서만 수행

**판정: 디자인 명세 위반.**

`ChatScrollArea.is_near_bottom()`은 구현돼 있지만, `_maybe_scroll_to_bottom()`은 이를 확인하지 않고 매번 scrollbar 최대값으로 이동한다. `디자인.md`는 사용자가 하단을 보고 있을 때만 새 메시지에 맞춰 스크롤하도록 명시한다.

**개선안:** 새 메시지 추가 직전의 하단 근접 상태를 저장하고, 하단을 보고 있던 경우에만 레이아웃 갱신 후 스크롤한다. 과거 메시지를 읽고 있는 동안은 현재 위치와 선택 상태를 유지한다.

### P2 — progress 문자열 대신 typed signal 사용

**판정: 현재 동작하지만 문구 변경에 취약.**

`ComicWorker`는 `progress(str, int, int)` 텍스트로 생성 단계와 패널 단계를 모두 보낸다. `MainController._panel_index_from_progress()`는 한국어 문구 정규식으로 컷 번호를 추출하고, 합성 시작도 문자열 포함 여부로 판단한다. 메시지 표현을 바꾸거나 번역하면 UI 단계 인식이 깨진다. 패널 재생성 worker에는 이미 `panel_started(int)` 신호가 있지만 일반 생성 worker에는 같은 구조가 없다.

**개선안:** `story_started`, `panel_started(index)`, `panel_completed(index, path, dialogue_status)`, `compose_started`, `finished`처럼 단계별 신호를 두고 텍스트는 표시 전용으로 둔다.

### P2 — 2×2 배치에서 이미지 비율 보존

**판정: 입력 크기가 달라지면 실제 왜곡. 현재 모델은 정사각형 설정이 많아 잠복 문제.**

`make_2x2()`는 모든 이미지를 `(max_width, max_height)`로 강제 resize한다. 직사각형 패널이나 외부 이미지가 들어오면 인물·배경 비율이 달라진다.

**개선안:** 각 셀 크기를 정하고 비율을 유지한 축소 후 남는 영역을 letterbox로 채운다. 셀 정렬·배경색은 고정하고 원본 이미지 크기와 관계없이 출력 크기를 보장한다.

### P3 — 폰트 설정을 Pillow fallback까지 전달하고 캐시 키 보정

**판정: 외부 리뷰의 핵심 보정이 정확.**

`compose.bubble.find_font(size)`는 명시적 경로 인자를 받지 않고 Windows/Linux 고정 후보만 찾는다. `find_font_path(explicit)`는 설정 경로를 처리하지만 Pillow 렌더 경로에서 호출하지 않는다. `_FONT_CACHE`도 크기만 키로 쓰므로 폰트 경로가 바뀌어도 기존 객체를 재사용한다. 추가로 `ComicService`가 `ComposeService()`를 기본 크기로 생성해 `config.json`의 `bubble_font_size`가 fallback에 적용되지 않는다.

**개선안:** 폰트 경로와 크기를 명시적으로 넘기는 API로 통일한다. 폰트를 실제 선택한 경로와 크기로 캐시하고 설정 변경 때 캐시를 무효화한다. fallback 폰트·크기를 사용자가 정한 설정과 연결한다.

### P3 — prompt 조건 충돌과 텍스트 생성 정책 조정

**판정: 설정별 이미지 품질 문제 가능성.**

`WorkflowAdapter.prepare()`는 모든 prompt에 말풍선 생성 문구를 추가하고 negative prompt에는 `text`, `letters`를 넣는다. dialogue를 이미지 안에 직접 생성하려는 사용자 workflow·모델에서는 대사 텍스트 억제와 충돌할 수 있다. 또한 positive에는 장면 배경을 보존하라고 하면서 negative에는 `blank background`, `plain white background`, `pure white background` 등을 기본 추가한다. 장면에 흰 배경이 필요한 경우에도 서로 경쟁하는 조건이 된다.

**개선안:** 말풍선 생성과 텍스트 금지 정책을 프로필 옵션 또는 명시적 생성 모드로 관리한다. 배경 관련 negative는 사용자의 장면 설명 및 프로필 설정과 충돌하지 않도록 조건부로 적용하고, 명시적인 흰 배경은 차단하지 않는다.

### P3 — ComfyUI workflow 오류 상태 조건을 명확히 하기

**판정: 논리 우선순위 때문에 오류 탐지가 불완전할 수 있음.**

`ComfyUIClient.wait_for_image()` 조건은 `status_str == "error" or completed is False and messages`다. Python 연산자 우선순위상 두 번째 조건은 `completed is False`이면서 `messages`가 truthy일 때만 성립한다. 완료되지 않은 오류 응답에 messages가 없거나 형식이 다른 경우에는 오류가 바로 보고되지 않을 수 있다.

**개선안:** 상태 문자열, `completed`, 오류 메시지 유무를 각각 검증하는 명시적 분기로 바꾸고, 인식되지 않은 종료 상태는 제한 시간까지 무의미하게 대기하지 않도록 한다.

### P3 — 업로드 입력 검증과 BubbleDetector 인자 전달 개선

**판정: 방어성 개선. 원격 코드 실행 취약점으로 과장하지 말 것.**

`upload_image()`는 `Path.open()`을 바로 수행하고 MIME을 항상 `image/png`로 지정한다. 현재 호출부는 자체 생성 PNG를 사용하지만, 잘못된 경로·파일 형식에 대한 오류 메시지는 부족하다. `BubbleDetector.detect()`는 경로를 Python `-c` 코드 문자열에 직접 삽입해 작은따옴표가 포함된 경로에서 스크립트가 깨질 수 있다. 로컬에서 선택한 파일 경로가 로컬 subprocess에 전달되는 형태이므로 이를 원격 RCE로 분류하는 것은 과도하다.

**개선안:** 업로드 전에 존재·파일 여부·허용 이미지 형식을 검사하고 MIME을 실제 형식에 맞춘다. detector는 `--model`, `--image` 인자를 subprocess argv로 전달하거나 표준 입력으로 전달해 코드 문자열과 데이터를 분리한다.

### P3 — 저장 I/O 빈도와 atomicity 개선

**판정: 성능보다 누적 비용·복구 일관성이 우선인 개선.**

`SessionManager.update()`는 `add()`를 통해 `save()`를 호출한다. `save()`는 매번 전체 `sessions.json`을 기록하고 모든 세션의 `session.json`도 순회 기록한다. progress·패널 상태 변경 때 UI 스레드에서 동기적으로 수행된다. 파일 수와 세션이 적으면 단일 저장은 짧을 수 있으나 세션 수·메시지가 늘면 매번 전체 직렬화와 여러 파일 쓰기가 반복된다. 중앙 목록은 임시 파일 교체를 쓰지만, `SettingsManager.save()`는 대상 JSON을 직접 덮어쓴다.

**개선안:** 진행 상태 저장은 완료 경계 중심으로 묶고, 필요하면 500~1000ms 디바운스 또는 별도 저장 worker를 적용한다. 저장 순서와 종료 시 flush를 명확히 한다. 설정 파일도 임시 파일 교체로 기록한다. 성능 개선 전후는 세션 수·메시지 수를 바꾼 로컬 프로파일링으로 판단한다.

### P3 — 저장 경로의 이식성 개선

**판정: 다른 PC·프로젝트 폴더 이동 시 깨질 가능성.**

세션 안의 결과 경로와 패널 이미지 경로가 절대 경로로 저장될 수 있다. 현재 `sessions.json`에도 절대 경로 데이터가 존재한다. 프로젝트 폴더를 옮기면 경로가 원래 PC를 가리켜 결과 미리보기나 재생성이 실패한다.

**개선안:** 프로젝트 루트 기준 상대 경로를 저장하고, 로드 시 경로를 해석한다. 기존 절대 경로 세션을 위한 마이그레이션 규칙과 파일 누락 UI를 둔다. 파일이 프로젝트 루트 밖에 있을 때의 저장 정책을 정의한다.

### P3 — Git 추적 및 의존성 재현성 정리

**판정: 저장소 운영 문제이며 현재 workflow 명령도 프로젝트 구조와 맞지 않는다.**

`.gitignore`에 `.github/`와 `*_ui.py`가 있다. 현재 `.github/workflows/ci.yml`, `quality.yml`은 존재하지만 추적 파일 목록에는 없고 ignore 대상이다. 두 workflow는 `novel_studio` 디렉터리를 compile/mypy 대상으로 지정하지만 현재 저장소에는 해당 디렉터리가 없다. `quality.yml`은 저장소에 없는 `requirements-dev.txt`도 설치 대상으로 지정한다. 따라서 workflow를 ignore 해제해도 현재 상태로는 CI가 정상 동작하지 않을 가능성이 높다. 반면 `ui/settings/settings_window_ui.py`는 이미 추적 중이라 ignore 규칙과 실제 관리 상태가 일치하지 않는다. `requirements.txt`는 최소 버전만 지정해 동일 환경 재현성이 제한된다.

**개선안:** CI workflow를 저장소에 포함할지 정책을 결정하고, 포함한다면 테스트 경로를 현재 `tests/`와 실제 앱 모듈에 맞추고 의존성 명세를 `requirements.txt` 또는 새 개발 의존성 파일과 일치시킨다. Qt Designer 산출물의 원본·생성 규칙을 문서화하고 필요한 생성 파일만 일관되게 관리한다. 앱 배포 기준 Python 버전과 의존성 lock 또는 검증된 버전 범위를 정한다.

### 별도 기능 트랙 — LM Studio 스토리 응답 스트리밍 표시

`LMStudioClient`는 SSE를 읽지만 누적 결과를 반환하며 UI에는 최종 응답 뒤 요약 카드가 나타난다. 현재 `디자인.md`는 원문 JSON을 직접 노출하지 않고 UI 요약만 단계적으로 표시하라고 하므로, 이는 현 스펙의 버그가 아니라 사용자가 체감하는 대기 시간을 줄이는 기능 개선이다. 스트리밍을 추가한다면 원문을 노출하지 않고 구조화된 진행 이벤트만 UI에 전달한다.

## 기존 재검토 항목 판정표

| 항목 | 이번 판정 | 정정·보충 |
|---|---|---|
| 1 대사 합성 실패 표시 | 부분 인정 | 최종물은 Pillow fallback 가능. 패널 완료 문구·상태가 과장됨 |
| 2 stage2 파일 | 조건부 인정 | 기본 프로필은 파일 존재. 누락된 커스텀 프로필만 불필요한 detect/upload 후 실패 |
| 3 패널 재생성 저장 불일치 | 인정 | compose 실패뿐 아니라 panel 완료 저장 후 취소에도 발생 가능 |
| 4 세션 저장 동기 I/O | 인정, 보통 수준 | UI thread에서 전체 세션 및 세션별 snapshot 쓰기 |
| 5 취소 중 HTTP 호출 | 인정 | timeout 3초, worker의 중복 interrupt 경로도 있음 |
| 6 전체 재생성 설정 재사용 | 인정 | 저장된 prompt/model 스냅샷 대신 라벨로 style 재조립 |
| 7 세션 손상 복구 | 인정 | 한 항목의 역직렬화 예외가 전체 목록 로드를 중단 |
| 8 자동 스크롤 | 인정 | 하단 여부 함수가 실제 자동 스크롤에서 미사용 |
| 9 progress 문자열 파싱 | 인정 | 단계 signal로 대체 권장 |
| 10 2×2 비율 | 인정 | 정사각형이 아닌 입력에서 왜곡 |
| 11 BubbleDetector 주입 | 부분 인정 | 경로의 따옴표로 코드 문자열 오류 가능. 원격 RCE 분류는 부적절 |
| 12 workflow 캐시 | 낮은 우선순위 | 매 컷 JSON 읽기와 deepcopy. 수정 시 매 실행 독립 복사본 보장 |
| 13 저장 디바운스 | 낮은 우선순위 | 전체 세션/프로젝트 snapshot 반복 쓰기까지 포함해 측정 후 조정 |
| 14 폰트 캐시 | 보충 인정 | 명시 폰트 설정 미전달, fallback 크기 설정 미적용이 선행 문제 |
| 15 gitignore | 인정 | 실제 `.github/workflows`가 존재하지만 ignore되어 추적되지 않음 |
| 16 절대 경로 | 인정 | 세션 이동·복사성 저하 |
| 17 의존성 범위 | 인정, 낮은 우선순위 | Python 버전과 lock 여부도 함께 정리 |
| 18A 스트리밍 UI | 버그 아님 | 현 디자인 명세에 부합, 별도 기능 트랙 |
| 18B prompt 충돌 | 인정, 조건부 | 사용자·장면 조건에 따라 white background 억제 가능 |
| 19 기존 완료 기능 | 회귀 방지 항목 | 수정 시 공용 prompt/seed, 세션 삭제 차단, 재생성 합성 등의 동작을 보존 |

## 권장 실행 순서

1. 재생성 결과의 단일 커밋 지점 마련. 취소·합성 실패 때 기존 결과 보존.
2. 일반 생성에서 계획과 패널 진행 상태를 복구 가능한 단위로 저장.
3. 취소 경로의 interrupt 호출을 worker 스레드 한 곳으로 이동.
4. 세션·설정 JSON 항목별 검증과 손상 백업 도입.
5. 대사 합성 상태 전달 및 stage2 경로 사전 검증.
6. 저장된 생성 설정으로 전체 재생성을 복원하고 typed progress signal 도입.
7. 자동 스크롤, 2×2 비율, 폰트 설정 문제 수정.
8. workflow/prompt 설정, 경로 이식성, 저장 성능, 저장소 ignore·의존성 정책을 다룬다.

## 회귀 검증 제안

수정에 들어갈 때 기존 테스트를 실행하고, 다음 실패 경로를 테스트로 추가하는 것이 좋다. 이번 검토에서는 실행하지 않았다.

- stage2 없는 커스텀 프로필에서 YOLO·upload를 시작하기 전에 비활성 경로로 분기하는지
- stage2 실패 시 fallback 결과와 UI 상태가 구분되는지
- 개별 패널 재생성 후 합성 실패·취소 때 세션과 최종 이미지가 이전 revision으로 일치하는지
- 생성 도중 재시작해도 계획과 완료된 패널이 복원되는지
- 한 세션 레코드만 손상된 JSON에서 나머지 세션을 복원하는지
- 취소 버튼의 UI thread가 네트워크 timeout 동안 막히지 않는지
- 비정사각형 이미지에서도 2×2 셀의 원본 비율이 유지되는지
- 폰트 경로·크기 변경이 Pillow fallback에 반영되는지

기존 완료 기능인 Master Seed 공유, character/style prompt 공유, 세션 생성 중 삭제·이름 변경 차단, 개별 컷 재생성과 자동 최종 합성, composer 동작은 회귀 검증 대상에 포함한다.
